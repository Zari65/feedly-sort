# feedly-sort

## What it does

This extension just includes:

* a content script, "feedly-sort.js", that is injected into any pages
under "feedly.com/" or any of its subdomains

The content script auto-sorts adds a sort button to the button bar.

## What it shows

* how to inject content scripts declaratively using manifest.json
